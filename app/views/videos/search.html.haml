%header
  %h3 your search result: (count: #{@videos.size}, key-words: '#{params[:search_pattern]}')
.videos.row        
  - @videos.each do |video|
    .video.col-sm-2(style="padding-top:10px")           
      = link_to video_path(video), nil do
        %img(src="#{video.small_cover_url}") 
